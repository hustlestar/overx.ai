{
  "buildCommand": "cd sites/main && npm run build",
  "outputDirectory": "sites/main/.next",
  "installCommand": "npm install --force && cd sites/main && npm install --force",
  "framework": "nextjs"
}